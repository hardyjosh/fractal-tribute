import { parseEther } from "viem";

export const CHANGES_PER_MOVE = 20;
export const price = parseEther("0.001");
export const ADDITIONAL_MINT_PERIOD = 1800 * 1000 // 1000 * 60 * 60 * 24; // 1 day in milliseconds
